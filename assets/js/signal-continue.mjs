const token=location.pathname.match(/^\/s\/([A-Za-z0-9_-]{43})\/?$/)?.[1];
const step=document.querySelector('#step'),status=document.querySelector('#status'),actions=document.querySelector('#actions');let state=null,busy=false;
async function request(payload){if(busy)return;busy=true;document.querySelectorAll('button').forEach(b=>b.disabled=true);status.textContent='Saving…';try{const r=await fetch('/api/signal/continue',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,...payload}),cache:'no-store',referrerPolicy:'no-referrer'});const d=await r.json();if(!r.ok)throw Error(d.error?.message||'Please retry.');state=d;render();status.textContent='';}catch(e){status.textContent=e.message;const retry=document.createElement('button');retry.textContent='Resume safely';retry.onclick=()=>request({action:'resume'});step.replaceChildren(retry);}finally{busy=false;document.querySelectorAll('button').forEach(b=>b.disabled=false);}}
function render(){step.replaceChildren();if(state.saved_answers?.length){const review=document.createElement('details'),title=document.createElement('summary');title.textContent='Review saved answers';review.append(title);for(const a of state.saved_answers){const p=document.createElement('p');p.textContent=a.question+' — '+a.answer;review.append(p);}step.append(review);}actions.hidden=!!state.done;if(state.done){const p=document.createElement('p');p.textContent=state.message;step.append(p);return;}const h=document.createElement('h2');h.textContent=state.question.prompt;step.append(h);for(const o of state.question.options){const b=document.createElement('button');b.textContent=o.label;b.onclick=()=>request({revision:state.revision,question_id:state.question.id,code:o.code});step.append(b);}}
document.querySelector('#start').onclick=()=>request({action:'resume'});
actions.onclick=e=>{const action=e.target.dataset.action;if(action==='later'){const label=document.createElement('label');label.textContent='When would you like Dylan to reconnect? (Optional)';const date=document.createElement('input');date.type='date';date.min=new Date().toISOString().slice(0,10);label.append(date);const b=document.createElement('button');b.textContent='Save future timing';b.onclick=()=>request({action:'later',revision:state.revision,...(date.value?{future_date:date.value}:{})});step.replaceChildren(label,b);actions.hidden=true;}else if(action)request({action,revision:state.revision});};
document.querySelector('#pause').onclick=()=>{actions.hidden=true;step.textContent='Your submitted answers are saved. Open this link again when it suits you.';};
if(!token){step.textContent='Open the secure check-in link Dylan sent you.';}
