# Canonical web → Signal Continue continuity

2026-09-26. Local certification **PASS WITH LIMITATION**; overall entry rollout remains **BLOCKED** on the hosted 408 transport gate.

## Eligibility and identity

A canonical distribution contact handoff records its phase and exact SMS relationship hash using existing RingCentral phone/hash configuration. This creates no SMS conversation, message, token, additional opportunity or marketing consent. Missing configuration leaves the optional relationship unavailable; acquisition and producer receipt still work. No new variables, secrets or migrations were added or changed.

Continue may use that relationship only when all these conditions hold:

- Population is WEB_DIRECT, with no district pilot source.
- Source is a canonical versioned distribution contact handoff and contact was explicitly requested.
- Current opportunity contact still resolves to the recorded relationship hash.
- No district enrollment or CONTROL roster owns that relationship.
- No other opportunity/workspace or unknown existing conversation is attached.
- An existing first-party guided session is not active, and contact is not suppressed.
- Existing Continue rules confirm producer choice, interest, interrupted discovery, known personal-lines product, and missing useful evidence.

Preview has no conversation side effect. A new conversation is created only when the producer prepares a valid draft; it is bound to that exact opportunity/workspace and paused under producer ownership. The first-party origin is recorded separately. An existing unbound thread is never auto-claimed from phone similarity. There is no broad enablement of all WEB_DIRECT records. Clear CALL, callback, Future Bind, urgent/coverage advice, negative and STOP behavior keeps its existing protection.

## Evidence and attribution

The same canonical distribution initial evidence and submitted answer signals hydrate Continue, alongside current source facts and existing SMS evidence. Freshness validation remains in force. Imported known timing is not asked again. Audience/profession metadata never becomes evidence. A missing/unknown product in legacy structured evidence cannot overwrite a known product.

Original lead source, route, campaign and first-touch timestamp are unchanged. The Continue source records its relationship and copies first-touch attribution for lineage. Current channel changes to signal_continue after approved sending, not merely preparing a draft. Completion saves evidence on the existing opportunity and retains one next action. Producer Workspace shows acquisition context separately from current channel and shows the completed Continue action. It does not expose district SMS controls to WEB_DIRECT.

## Regression evidence

Full CoverageFit suite: **295 pass, 0 fail, 0 skip**. Added coverage includes:

1. Actual canonical buyer first answer → explicit contact → producer projection → Continue draft → fake-provider approval → resume → Future Bind on the same opportunity.
2. Original buyer route/campaign and full original source record remain unchanged.
3. Known first answer and canonical timing survive the channel change.
4. Preview creates no conversation; draft creates no send.
5. Producer-owned conversation remains human-only.
6. Unknown thread and another opportunity/workspace are denied.
7. Changed phone identity is denied.
8. Active web/guided flows are denied.
9. Existing CONTROL relationship is denied, including CONTROL enrolled after a token was created.
10. Suppression invalidates the continuation.
11. An already clear next action creates neither conversation nor token.
12. Existing CONTROL, district SIGNAL, STOP, revision conflict and review-first/exactly-once tests continue to pass.

Provider sends in new integration tests are explicitly stubbed. No real SMS was sent. Hosted cross-channel and provider certification are still required using a user-controlled test identity. Legacy web records without an exact canonical relationship remain unavailable; no silent backfill or fuzzy reconciliation is performed.

## Operator behavior

After an interrupted producer call, open an eligible WEB / DIRECT opportunity, choose Continue async, confirm interest and async preference, prepare the draft, review, then approve. Keep a clear callback or Future Bind as its existing action. Completion appears on the same opportunity; no answer copying is required.

No district cohort or priority weights changed. No autonomous link sending, reminders or AgencyZoom writes were added. The hosted 408 presentation failure is separate and still blocks public route activation; inspect the existing entry_presentation_upstream_failure runtime log before changing configuration.
